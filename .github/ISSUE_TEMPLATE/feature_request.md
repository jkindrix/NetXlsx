---
name: Feature request
about: A capability you want on the public surface
labels: enhancement
---

## The capability

<!-- What you want to do, ideally as the code you wish compiled. -->

## Current workaround

<!-- Most OOXML surfaces are reachable today through `.Underlying`
     (the raw Open XML SDK escape hatch) — if you tried that, show it;
     it's the best evidence for what the wrapped API should look like. -->

## Roadmap check

<!-- docs/roadmap.md has a binary feature matrix incl. an explicit Never
     column. If your ask is already there (any column), link the row —
     a `Never` rationale that no longer applies is worth re-opening. -->
