# Remediation ledger — 2026-06-10 full-repo review

**Status:** ACTIVE — this is the single authoritative tracking surface for the
2026-06-10 review findings (4-agent audit + live repros + a 4-consumer interop
gauntlet: LibreOffice 26.2.4, openpyxl 3.1.5, python-calamine, NetXlsx
self-read, in both directions). Every finding lives here until it is **drained**
into the structure that owns it — a design row (I-NN) for API changes, a
roadmap row for features, a slice commit for fixes, or an explicit
WON'T-FIX with rationale. When the ledger is empty, this file gets a
HISTORICAL banner like the retired spike docs. It does not compete with
`roadmap.md`/`design.md`; it feeds them.

**Verification pass 2026-06-10 (independent, same day):** every item below was
re-verified against HEAD 0999b1f with fresh repros (atomic-save destruction
2183→0 bytes; control-char failure shape; positional-record CS7036/CS1061 from
a clean project; crafted formula-cached cells injected into saved output;
sheet-name probes), two documentation/CI audit sweeps, and primary-source
ecosystem checks. The findings held, with four corrections edited in place
below (R-3 SpreadCheetah claim, R-8 theme overstatement, R-9 colon gap, R-14
spec-legal reframe), one item rewritten to its true scope (R-7 → design-I7
conformance), two NEW items (R-35 dispose-poisoning — joins S1; R-36 issue/PR
templates decision), and evidence upgrades where probes landed. Suite at
0999b1f measures **1472**+35+18+1 per TFM (not 1471) — the docs truth pass
(R-17) should re-measure rather than copy any count.

**Process rules for draining this ledger** (mirrors the v1.1+ working
agreement):

1. One slice at a time, fully done — implemented, tested, CHANGELOG'd,
   verified by a second pass — before the next starts (charter §1.1).
2. Any item marked **[I-NN first]** requires a design-row proposal and
   operator sign-off BEFORE code. Propose the shape; do not implement on
   the same session as the proposal.
3. Items marked **[golden-regen]** change emitted bytes — the slice must
   regenerate golden fixtures deliberately and call that out in its commit.
4. Status values: `OPEN` → `IN-SLICE-<n>` → `DONE @<commit>` /
   `WONT-FIX (<rationale>)` / `GRADUATED → <I-NN | roadmap row>`.
   Nothing is deleted; statuses are edited in place.
5. Verification evidence for each DONE item: name the test(s) that pin it
   and, where interop-relevant, the oracle (LO / openpyxl / calamine) that
   confirms it.

Verification tags below: **[verified]** = reproduced live during the review;
**[confirmed]** = checked in source/output; **[reported]** = agent finding,
plausible, needs in-slice verification before fixing.

---

## A. Product defects (fix first — data integrity, then correctness)

| ID | Sev | Status | Finding | Evidence | Done when |
|----|-----|--------|---------|----------|-----------|
| R-1 | **Critical** | DONE @6926552 | `Save(path)` is non-atomic: `File.Create` truncates the destination before serialization; any failure destroys a pre-existing file (0-byte husk reproduced). | [verified] `OoxmlWorkbook.cs:539`; stream overload already memory-buffers (`:512-529`) | Fixed: sibling-temp + same-volume rename (`AtomicFileWriter`). Pinned by `SaveFailureModeTests.FailedSavePath_LeavesPreexistingDestinationByteIntact` / `SavePath_OverwritesExistingFile` / `SavePath_MissingDirectory_Throws_AndCreatesNothing` (+ no-temp-residue asserts). |
| R-2 | **Critical** | DONE @6926552 | Streaming `Save(path)` has the same truncate-first exposure. NOTE (verification pass): the truncate-first there is DELIBERATE — the code comment says a bad path must fail before the one-shot finalize burns the workbook's only Save — so the fix must preserve that property. | [confirmed] `OoxmlStreamingWorkbook.cs:86` (comment at `:84-85`); `Save(Stream)` already assembles via a temp file | Temp file **in the destination directory, with the destination filename embedded in the temp name** (validates dir existence/perms AND filename legality before finalize), then `File.Move(overwrite)` — same-volume atomic rename. Residual (documented, acceptable): dest-is-a-directory / dest-locked surface at Move, after finalize. Test: bad-dir save throws BEFORE finalize and a subsequent good-path save still succeeds (single-shot not burned). Same destination-intact test shape as R-1. Implemented exactly as specified; pinned by `SaveFailureModeTests.StreamingSavePath_MissingDirectory_FailsBeforeFinalize_SaveNotBurned` / `StreamingSavePath_OverwritesExistingFile`. |
| R-3 | **Major** | DONE @f980076 — I-88 landed as signed off: ST_Xstring escape (incl. 0x0D) on content surfaces both engines + decode on every read path; fail-fast for sheet names (all control chars — attribute normalization) and formulas (XML-invalid → FormulaException at setter); ValidateDefinedName verified already-excluding; LO 26.2 oracle = lossless both ways, openpyxl = literal text (documented); 54 tests; save-failure tests repoisoned via the hatch | Control chars (0x00–0x08, 0x0B-0x0C, 0x0E-0x1F) in `SetString`/`Comment` (and presumably rich text, sheet names via other paths) surface only at `Save` as a raw `ArgumentException` with no cell context, outside the `WorkbookException` family. Combined with R-1 = data loss. Also the SDK's #2-upvoted authoring pain (positioning gift). | [verified] live repro; tab/LF/emoji round-trip fine. AMENDED (2026-06-11 external-review pass, repro-confirmed both engines): **CR (0x0D) is SILENTLY DESTROYED today** — `"C\rD"`→`C<LF>D`, `"C\r\nD"`→`C<LF>D` (XmlWriter NewLineHandling + XML 1.0 §2.11); no test pins `\r`; MS-OI29500 says CR "shall be escaped" despite being XML-valid — so R-3 is not only late-throw but silent-mutation, and the I-88 escape set must include 0x0D | **[I-NN first]** — two candidate shapes: (a) fail-fast at the setter naming the cell; (b) OOXML `_xHHHH_` escape convention per ECMA-376 ST_Xstring §22.9.2.19 (lossless, what Excel itself does). CORRECTED (verification pass): SpreadCheetah does NOT escape — it silently STRIPS 0x00–0x1F (lossy; verified in its `XmlUtility.cs` + tests), and the raw SDK throws — so a lossless (b) would exceed every library in the set, not merely match. Also temper the "SDK's #2-upvoted" framing: the rank is right (issue #1532) but absolute reactions are modest (5; the #1, #244/2GB, has 13). Recommendation: (b) for `SetString`/rich text, (a) for names/formulas. Tests: control chars, surrogate halves, U+FFFE/FFFF. |
| R-4 | **Major** | DONE @0e7c467 — ReadRows constructs via the record primary ctor (named args; initializer suffix for extra mapped body props; defaulted unmapped params omitted); value-type `[Worksheet]` types also lose AddRow's `record is null` guard (was CS0037 — same silent-emit class, found in-slice); genuinely unconstructible shapes fire new **NXLS0007** (fatal); pinned by `PositionalRecordEmissionTests` incl. compile-AND-run AddRows→Save→Open→ReadRows round-trip | Source-gen `ReadRows` emits **non-compiling** code for positional records (CS7036 in the `.g.cs`, object-initializer vs required primary-ctor params) and **no diagnostic fires** (NXLS0002 deliberately exempts primary ctors). The README's flagship example (`README.md:164-175`) is exactly this shape. `AddRows` works; the asymmetry is silent. | [verified] live repro; cookbook works only because `SalesRecord` uses body init-props | Generator constructs via the primary ctor (or designated ctor) when properties are positional; emission test that **compiles and runs** a positional record through AddRows→ReadRows; new diagnostic for genuinely unconstructible shapes; AnalyzerReleases entry. |
| R-5 | **Major** | DONE @c7f9451 — decided: README fixed to the non-generic call shape (a generic alias would be fake generics — each type's extension is per-type; AOT story unchanged); typed-mapping example rewritten incl. the hand-authored header row and compile-and-run verified verbatim; README:17 bullet corrected | Emitted `ReadRows()` is non-generic; README documents `ReadRows<SalesRow>()`, which does not exist. | [verified] CS1061 repro + `TypedImport.cs:47` uses non-generic | Decide: fix README to non-generic (cheap) or also emit a generic alias. Fix README's typed-mapping section either way (with R-4). |
| R-6 | **Major** | DONE @42206d7 — GetTime/GetDuration round to nearest ms (shared rounding, overflow-guarded); boundary pinned: round-to-24:00:00 → GetTime null / GetDuration 24h | `GetTime`/`GetDuration` truncate serial fractions instead of rounding — LO-authored `0.396006944444444` (= 9:30:15 at 15 digits) reads back 9:30:14.9999996. Internally inconsistent: `GetDate`→`FromSerial` rounds to ms (`OoxmlWorkbook.cs:119`), display path rounds (`ExcelDateFormat.cs:327`), so `GetString()` and `GetTime()` disagree on the same cell. Self-tests can't catch it (own writer emits 17 digits). | [verified] LO round-trip + arithmetic | Round serial→`TimeOnly`/`TimeSpan` to nearest millisecond (reuse `RoundToMillisecond`). Tests: 15-digit serial fixtures from LO/Excel-style writers, PLUS the post-rounding boundary (a serial like 0.99999999 rounds to 24:00:00, unrepresentable in `TimeOnly` — pin whether that returns null or 00:00). Re-verified 2026-06-10 by crafted-serial injection: `GetTime` → 09:30:14.9999999. |
| R-7 | **Major** | DONE @42206d7 — EffectiveKindOf classifies formula cells by cached result; all typed getters + GetString error-literal arm per I7 verbatim; public Kind unchanged (Formula); fresh-formula/empty-`<v/>` shapes pinned; no precedence ambiguity surfaced in-slice (the `t` attribute fully determines the cached type); compile-and-run ReadRows-over-formula-column proof in `FormulaCachedValueTests` | REWRITTEN (verification pass — the original bool finding is the tip): **formula-cached results are unreadable across the ENTIRE typed-getter surface, violating design decision I7 verbatim** (`design.md:88`: GetString returns "formula → cached result as string; error → error code"). `KindOf` short-circuits to `Formula` before checking `t` (`OoxmlCell.cs:67`), and the getter switches have no `Formula`/`Error` arms → `GetNumber` on `<f>1+1</f><v>2</v>` = null, `GetString` on a formula-string = `""`, `GetString` on an error cell = `""` (I7 says `"#DIV/0!"`), `GetBool` on LO's `t="b"`+`<f/>`+`<v>1</v>` = null. Only `GetError` reads cached results (#49); internal `TextForMeasurement` (`OoxmlCell.cs:298-306`) reads them fine, so AutoSize sees values users can't. Self-tests can't catch it — own writer never stores cached `<v>` (#46). Knock-on: generated `ReadRows` does `GetNumber() ?? throw`, so **typed import throws on any Excel-authored file with a formula column** — guts the R-34 migration story. | [verified] crafted-cell probes 2026-06-10 (`<f>1+1</f><v>2</v>`→null, formula-str→`""`, error→`""`, `t="b"` cached→null; `GetError`→DivByZero ✓) | **I7 conformance, not new design**: `GetString` returns cached result for formula cells and the error literal for error cells; `GetNumber`/`GetBool`/`GetDate`/`GetTime`/`GetDuration` honor cached `<v>` with `t`-appropriate typing. Fixtures: Excel/LO-authored formula cells (the crafted-cell zip-injection pattern works); a compile-and-run `ReadRows` test over a formula column. If any precedence question turns out genuinely ambiguous in-slice, surface it rather than improvise. |
| R-8 | Medium | DONE @940164d — I-89 landed (slice S11): lazy default-theme embed via the `EnsureThemePart` choke point (style-pool hook + picture-border/connector/pie-chart/rich-run guards; streaming embeds at Save-time assembly; source-scan test enforces the discipline), `Workbook.DefaultThemeXml` (byte-transcribed Excel-authored Office theme, accent1 #4472C4), and the styling symmetry — `CellStyle.FontColorTheme`, `RichTextStyle.ColorTheme`, `CellBorders.{Top,Right,Bottom,Left}ColorTheme` with theme+tint read-back. Plain workbooks stay byte-identical (goldens untouched). Lottery inverted, verified vs LO 26.2: the embedded theme is PRESERVED through resave, theme-4 = #FF4472C4 before AND after, the flattened picture border carries the correct Office literal; openpyxl reads theme/tint. Probe theme asserts promoted soft→hard; interop.md re-baselined. Two judgment calls surfaced for review: pie-chart accent fills treated as a trigger site (the memo's normative "every theme-color write" sentence; its illustrative list omitted charts), and truthful read-back surfaces font-0's `theme="1"` on styles read from default-font xfs (re-applying such a read style embeds — pinned in `ThemeStylingSymmetryTests`). Also closes XlsxCodeGen Appendix A #1 (read-back included). **JUDGMENT CALLS RATIFIED 2026-06-18 (operator, S18): both correct — pie-chart accent fills are a legitimate theme-embed trigger (only chart type emitting `schemeClr`, per the memo's normative rule); truthful font-0 `theme="1"` read-back preferred over lossy suppression (over-report is benign, suppression would mis-report genuine theme-1 fonts). The carried "I-89 has no design.md §6 fold" flag is CLOSED: `design.md §6.12.4` written @72ad4d0 (S18), matching the §6.12.1–.3 register, recording both ratified calls.** | CORRECTED (verification pass): the original "no theme part is ever written" overstated — **`SetThemeXml(byte[])` (I-79), `GetThemeXml()` and `ResolveThemeColor` (I-81) exist as shipped public API with a real write path** (`OoxmlWorkbook.Theme.cs:48` `AddNewPart<ThemePart>()`), and the XML docs already warn about consumer fallback. The true gap: **`Create()` embeds no DEFAULT theme while `ThemeColor` styling is accepted without one** → theme-indexed colors are a consumer lottery — proven: LO injected its own theme (theme-4 resolves `#FF18A303` green vs Office blue) and rewrote the theme-indexed picture border to literal white; `ResolveThemeColor(4)` = null on own fresh output (per the documented I-81 contract). | [verified] part listing + LO round-trip; API surface re-checked 2026-06-10 | **[I-NN first]** — options now include a third shape: (a) embed a default Office theme at `Create()` (values are facts; hand-author the XML), (b) fail loud on `ThemeColor` without a theme, (c) ship a `DefaultTheme` constant/helper for the existing `SetThemeXml` and document the footgun on `ThemeColor`. Converges with the `CellStyle.FontColorTheme` ask (XlsxCodeGen Appendix A #1) — design as ONE theme cluster. |
| R-9 | Medium | DONE @8a2c419 — ':' + apostrophe-edge + reserved 'History' (case-insensitive) added to the shared char-array trio; Sanitize maps each to '_' (trailing-apostrophe fix runs post-truncation); CHANGELOG behavioral note; pinned by `SheetNameValidationTests` | Sheet-name validation narrower than the ecosystem: leading/trailing apostrophe accepted (`'Leading`); LibreOffice **silently renames it to `Sheet1`** on resave; Excel forbids it at entry. Reserved name `History` also unchecked. EXPANDED (verification pass): **the invalid-char set is missing `:` (colon)** — `s_invalidSheetNameChars` (`Workbook.cs:20`) is `{ \ / ? * [ ] }` while Excel's documented forbidden set is `/ \ ? * : [ ]`; `AddSheet("a:b")` ACCEPTED (probe), and colon collides with 3D-reference syntax — arguably the worst of the four gaps. All probes 2026-06-10: `'Leading`, `Trailing'`, `History`, `a:b` all ACCEPTED. | [verified] repro + LO round-trip + MS rename-a-worksheet docs | Tighten `ValidateSheetName`/`IsValidSheetName`/`SanitizeSheetName` (one shared char array feeds all three): add `:`, apostrophe rule, decide on `History`; CHANGELOG behavioral note; tests for all rules. |
| R-10 | Medium | DONE @98cac83 (I-91 slice 1, S14) — `ICell.RemoveHyperlink()` is the way off; `ICell.Hyperlink`/`SetString` XML docs now state the keep-link-on-text-edit semantics (Excel parity) and point at it; regression pinned (`Hyperlink`→`RemoveHyperlink`→`SetString` leaves no element, no rel) | `SetString` after `Hyperlink` leaves the rel targeting the old URL under new text (openpyxl-confirmed), and there is **no API to remove a hyperlink**. (Excel also keeps links on text edit — the defect is the missing removal + missing docs, not the keep.) | [verified] | Graduates into R-11's removal-API design row; until then, document the semantics on `ICell.Hyperlink`/`SetString`. |
| R-11 | Medium | DONE @b557e4c (I-91 complete) — **I-91 SIGNED OFF 2026-06-11** (S2 amended memo; folds in R-10). Slice 1 of 2 (cell/workbook level) DONE @98cac83 (S14): `ICell.RemoveHyperlink`/`RemoveComment`, `ISheet.RemoveValidation`, `IWorkbook.RemoveNamedRange` + the `OoxmlTable` stale-handle retrofit. Slice 2 of 2 (drawing layer) DONE @b557e4c (S15): `ISheet.RemovePicture`/`RemoveChart`/`RemoveShape`/`RemoveConnector` (RemoveTable semantics — foreign/stale → ArgumentException; anchor match by live element; shared-image-part refcount [A-2026-06-11]; DrawingsPart + `<drawing>` rel teardown when last anchor leaves), stale-handle retrofit onto `OoxmlPicture`/`OoxmlChart`/`OoxmlShape`/`OoxmlConnector`, design.md §6.12.3 fold covering BOTH slices, LO 26.2 gauntlet proves the shared-image survivor still renders. | Removal asymmetry across the surface: `UnmergeCells`/`Unprotect`/`RemoveTable`/`RemoveConditionalFormatting`/`ClearAutoFilter` exist; nothing removes hyperlinks, comments, pictures, charts, shapes, named ranges, or validations. | [confirmed] PublicAPI surface dump | **[I-NN first]** — one batch design row covering the removal family (shapes, naming, part/rel cleanup semantics). Implement in 1–2 slices after sign-off. |
| R-12 | **Major** (capability) | **DONE** — both slices landed; I-90 COMPLETE. Slice 1 (rename+move) **@81ce04e** (S12); slice 2 (delete) **@c004788** (S13). Slice 1: `ISheet.Rename` — a method per the memo (setter rejected); AddSheet rules; document-wide rewrite via the sheet-reference lexer over every memo surface (cell formulas incl. shared masters, defined names INCLUDING `_xlnm.*`, hyperlink locations [exceeds Excel, documented], CF/DV, chart `c:f`, pivot-cache `@sheet`, sparkline `xm:f`, table column formulas) with output-quoting normalization; residuals pinned by test — INDIRECT strings untouched (Excel parity), 3D spans untouched (diverges from Excel; surfaced as a judgment call). `IWorkbook.MoveSheet` — 1-based resulting position, remove-then-insert; `localSheetId` re-indexed; `activeTab` follows/clamps; calcChain untouched (`c/@i` is a sheetId, noted in-code). Slice 2: `IWorkbook.RemoveSheet(ISheet)` — RemoveTable precedent (ArgumentException on foreign/stale handle); last-VISIBLE-sheet guard (InvalidOperationException); WorksheetPart + owned descendants + `<sheet>`/rel deleted, calcChain dropped WHOLESALE, pivot caches sourced from the sheet removed; scoped defined names purged + later scopes re-indexed (purge-then-reindex); cross-sheet refs → `#REF!` via the shared lexer's new RewriteToRefError mode (same #/]/:/! guards; 3D-span residual stays); `activeTab` clamped (not followed); removed wrapper + its cells throw InvalidOperationException (distinct from ObjectDisposedException) via a unified `ThrowIfUnusable()` the ISheet partials + OoxmlCell now route through. Zip-level no-orphan/no-dangling-rel assert. LO 26.2 oracle: rename reference RESOLVES on LO load (cached 42); delete `#REF!` survives resave (LO lowercases to `#ref!` — normalization, soft-tolerated); name body/hyperlink/tab order survive; openpyxl clean both legs. Two documented residuals, both faithful to the memo (not divergences): on delete `activeTab` is CLAMPED, not followed (the memo specifies clamp for delete, follow for move); the removed-handle guard covers ISheet + its cells (the memo's named scope) — `IRow`/`IRange`/`IColumn`/`IPicture` handles are not themselves guarded, though cell-level ops through them still throw. **JUDGMENT CALLS RATIFIED 2026-06-18 (operator, S18): (1) totalsRowFormula rewrite alongside calculatedColumnFormula is a correct superset — Excel maintains both on rename; the memo's omission was incomplete enumeration, not exclusion. (2) 3D-span references (`Sheet1:Sheet3!A1`) left unrewritten on rename/delete is ACCEPTED as a documented residual — a genuine lexer limitation (the `:` predecessor guard prevents malformed output), low real-world likelihood, and the failure is LOUD (a dangling endpoint resolves to a visible `#REF!`, never a silently-wrong sum), pinned by `Rename_Leaves_3D_Span_References_Untouched_Documented_Residual`. Revisit only if 3D-reference workbooks become a known use case.** | No sheet rename / reorder / delete (`ISheet.Name` get-only; no `RemoveSheet`/`MoveSheet`) — basic capability every competitor has; roadmap matrix falsely marks it shipped v1.0. | [verified] | ~~**[I-NN first]**~~ I-90 **SIGNED OFF 2026-06-11** (S2 amended memo) answered all four: rename = method; rewrite included via lexer; move = 1-based resulting index; delete = part + rel + name cleanup (slice 2). Roadmap row corrected by R-17 regardless. |
| R-13 | Minor | DONE @42206d7 — DOM refreshes `<dimension>` from live extent at Save (fixes stale producer values too); streaming tracks extent at flush + splices before `<x:sheetData>` at assembly; openpyxl read-only verified live on both engines; NOTE: "[golden-regen]" turned out vacuous — no byte-exact goldens are committed (GoldenFiles = behavioral recipes), schema-validator gate green | Neither writer emits `<dimension>`; openpyxl read-only/streaming mode refuses the streaming output ("Worksheet is unsized"). Extent is knowable at save on both paths. | [verified] | **[golden-regen]** Emit `<dimension>` from both writers; regenerate goldens deliberately; pin with openpyxl-read-only-style expectation (dimension attribute present). |
| R-14 | Medium | DONE @8a2c419 — infer previous+1 at Open (Excel-compatible), materialized so every reader path sees one grid; c/@r-less cells swept in the same pass; present-but-corrupt refs untouched (I-83 fail-loud pinned); fixtures in `MissingReferenceNormalizationTests` | REFRAMED (verification pass): rows lacking `@r` are silently invisible on open (`OoxmlSheet.cs:119` cache build) — cells in such rows unreachable. **`row/@r` is OPTIONAL per ECMA-376 (absent = previous+1), so this is spec-legal third-party input silently losing data, not "malformed input"** — which makes it a sharper I-83 tension, though real-world `@r`-omitting writers are rare (Excel/LO/openpyxl always emit it). The skip is also deliberate and documented in-code (`OoxmlSheet.cs:113-118`, pre-cache parity rationale). | [verified] 2026-06-10 — injected `@r`-less row invisible, `LastRowNumber` stops before it | **Infer previous+1 (Excel-compatible)** — throwing would reject files Excel opens fine, i.e. be wronger than today; pin with a fixture. Same decision should sweep `c/@r`-less cells (also optional per spec) — see R-16's `EnumeratePopulated` item. |
| R-15 | Minor | DONE @8a2c419 — verified live (Dispose took no lock); strict-mode Dispose now under the per-workbook lock; default-mode race scope documented verbatim on `WorkbookOptions.StrictConcurrencyDetection`; pinned incl. an 8-thread dispose-vs-mutate race test | Default-mode concurrency: dispose-vs-mutate check-then-act window (`ThrowIfDisposed` before `EnterMutation`); strict mode's lock may not cover `Dispose` itself. Pre-verified (verification pass): the pattern is visible at `OoxmlWorkbook.cs:454-455`, and `Dispose()` (`:584-590`) takes no lock. | [reported] — verify in-slice | Cover `Dispose` under the strict lock; document the default mode's exact race scope in `WorkbookOptions.StrictConcurrencyDetection` docs. |
| R-16 | Minor | DONE @8a2c419 — all three verified: named-range body now passes the SetFormula structural gate (FormulaException, tests); EnumeratePopulated @r-less order fixed by R-14's normalization by construction; number-format codes WONT-VALIDATE by design (consumer-rendered, degrades to General; NXLS0003 covers compile-time) — documented on `ICell.NumberFormat` | Agent-reported, unverified leftovers to falsify-or-fix in one sweep: named-range formula accepted unvalidated (pre-verified: only null/empty guards, body unvalidated — `OoxmlWorkbook.Names.cs:32-36,67`); `EnumeratePopulated` trusts DOM cell order on `@r`-less input (note: `c/@r` is optional per spec — pair with R-14's decision); number-format codes unvalidated (probably document-only). | [reported] | Each gets verified → fixed, documented, or WONT-FIX'd with rationale in this ledger. |
| R-35 | **Critical** | DONE @6926552 | NEW (verification pass 2026-06-10): **a failed `Save` poisons the workbook so that `Dispose()` throws the same raw exception again, unhandled** — the SDK's autosave re-serializes inside `OpenXmlPackage.Dispose()` (stack via `OoxmlWorkbook.cs:588`). A `using` block around a failed save crashes at the closing brace and the Dispose exception MASKS the original. R-1's own acceptance test cannot be written until this is fixed (the verifying probe program crashed exactly this way). The 45-op disposal matrix covers ops-after-dispose, not dispose-after-poisoned-save — uncovered class. | [verified] live probe (`new.disposeAfterFailedSave=THROWS:ArgumentException`) | Open/create the `SpreadsheetDocument` with autosave OFF (the facade's `Save` is explicit; the backing is an internal stream, so autosave-on-dispose never had a user-visible effect to lose) — verify `Save(Stream)`'s explicit `_document.Save()` path is unaffected. Test: failed save → `Dispose()` clean → original exception observable → workbook still mutable+saveable after removing the offending content. Lands FIRST in S1. Implemented exactly as specified (autosave off at both DOM sites + explicit `doc.Save()` in streaming assembly); pinned by `SaveFailureModeTests.FailedSave_DisposeIsClean` / `FailedSave_WorkbookRemainsUsable_AfterRemovingOffendingContent` / `FailedSave_OnOpenedWorkbook_DisposeIsClean`. |

| R-37 | **Major** | DONE @b4ee24f — all three breach shapes classify to MalformedFileException naming the sheet; sibling rootless-worksheet-part NRE classified at the Worksheet accessor; exhaustive single-flip sweep post-fix: 0 breaches (was 9); pinned by 3 fixtures in `MalformedInputContractTests` + nightly-scale fuzz run green | NEW (2026-06-12, found by the S9 deep-fuzz harness on its FIRST scaled run): `Workbook.Open` throws a raw `NullReferenceException` — outside the documented exception contract — when a `<sheet>` element's `r:id` is corrupted/absent: `IndexExistingSheets` (`OoxmlWorkbook.cs:313`) does `sheet.Id!.Value!` under null-forgiveness. Exhaustive single-bit-flip sweep of a 2,221-byte workbook: 9 positions NRE. A missing-part id or a non-worksheet target in the same loop would surface as `ArgumentOutOfRangeException`/`InvalidCastException` — same contract breach class. | [verified] exhaustive single-flip sweep 2026-06-12; stack pinned to `OoxmlWorkbook.cs:313` | Guard the loop: null/empty `r:id`, unresolvable part id, and non-`WorksheetPart` target each classify to `MalformedFileException` naming the sheet; pinned by corrupt-`r:id` fixtures in `MalformedInputContractTests` + the deep-fuzz sweep run at scale locally. |
| R-38 | Medium | **DONE @daadf58 (I-92, S20) — signed off 2026-06-18 (operator chose all 4 recommendations), landed same session.** Shape `docs/r38-chartsheet-proposal-2026-06-18.md`, folded into `design.md §6.12.5`. A `<sheet>` targeting a `ChartsheetPart`/`DialogsheetPart` now opens as an `OoxmlChartsheet` placeholder (Option A skip-with-visibility): participates in `SheetCount`/`Sheets`/indexers, carries `Name`/`Hidden`, supports Rename/Move/Remove, round-trips byte-stable; every grid member throws `NotSupportedException`. New `ISheet.Kind` (`SheetKind {Worksheet,Chartsheet,Dialogsheet}`). Open path classifies 3 ways — WorksheetPart→OoxmlSheet, Chart/DialogsheetPart→placeholder, **anything else (e.g. styles part)→`MalformedFileException` unchanged** (message reworded "not a worksheet part"→"not a sheet part"; the misdirection test stays loud). Internals: sheet collection retyped to `IOoxmlSheet`; `SheetElementFor` generalized to `OpenXmlPart`; `UpdateDimension`/`InvalidateRowCache` loops skip non-worksheets; RemoveSheet tears the part down via `SheetPartInternal`. Verified by `ChartsheetTests` (SDK-authored chartsheet+dialogsheet fixtures: open/Kind, grid-throws, Hidden, rename/move/remove, byte-stable round-trip incl. part-gone-after-remove); suite 1785→1793 ×2 TFMs. **Fixture note:** built via the raw Open XML SDK (NetXlsx can't author chartsheets); an Excel/LO-authored binary fixture for the golden corpus remains a nice-to-have follow-up, not a blocker. | [confirmed] code path; chartsheet fixture now SDK-synthesized in ChartsheetTests | ~~**[I-NN first]** — propose the shape (skip-with-visibility vs wrapper) before code; add an Excel/LO-authored chartsheet fixture either way.~~ DONE: I-92 Option A signed off + landed S20 @daadf58. |

## B. Documentation truth pass (cheap, protects the project's core asset)

| ID | Sev | Status | Finding | Done when |
|----|-----|--------|---------|-----------|
| R-17 | **Major** (brand) | DONE @34c89c8 — status→v2.0.1; ClosedXML engine claim fixed + differentiation reframed; Fuzz in Layout; recipe claim scoped to v1.0/v1.1; decision counts de-quoted (no-drift fix, same as test counts) in README AND CONTRIBUTING; net9.0 runtime claims corrected (TFMs = net8.0+net10.0); typed-mapping example was already fixed by R-5 in S3 | README factual errors: Status line still "v2.0.0-alpha.1" (and README is the **packed NuGet readme**); comparison table claims ClosedXML uses "own OOXML impl" — false, ClosedXML sits on the Open XML SDK ≥3.1.1 (differentiation vs ClosedXML is thinness/AOT/streaming/source-gen, not engine); Layout omits `tests/NetXlsx.Fuzz`; "20 recipes covering every public-surface area" stale (nothing post-v1.1); decision counts stale (I-87 > "32"); typed-mapping example broken (R-4/R-5). EXPANDED (verification pass): **`CONTRIBUTING.md:7` carries a THIRD inconsistent decision count ("52 + 22")** — the truth pass must cover it too; and the live suite count is 1472/TFM at 0999b1f, so re-measure any count rather than copying one. | One README+CONTRIBUTING truth-pass slice; every claim re-verified against the repo at HEAD; typed-mapping example updated to a shape that compiles (after R-4/R-5 decide the shape). |
| R-18 | **Major** (process) | DONE @34c89c8 — matrix re-baselined row-by-row with a recorded verdict block (new v2.x column separates design-approved from shipped); 4 false-Yes rows corrected; 4 unshipped v2.0-Yes items got R-33 verdicts; all dead NPOI blockers rewritten (In(...)/Top-N/themes/threaded/streaming/encryption); v1.0 checklist banked as historical with named deviations (benchmark-vs-peers + GetValue<T> never landed) | Roadmap needs the post-swap re-baseline its own `long-term.md` prescribes: false v1.0 "Yes" rows (sheet rename/reorder/delete — R-12; ILogger — zero references in src); v2.0 shipped with matrix-Yes items unimplemented (themes-as-styling, threaded comments, streaming read, Roslyn analyzers) vs a DoD requiring them; v1.0 progress checklist never checked; ≥6 deferment rationales cite "Blocked on NPOI 3.x" for an engine that no longer exists (themes, threaded comments, streaming read — SDK has `OpenXmlPartReader`, Top-N filters, `In(...)` 3+, encryption). | Matrix re-baselined row-by-row with verdicts (promote/demote/hold + rationale), per the long-term.md method; dead NPOI blockers rewritten to current truth; drift mechanism noted (matrix edits require the release-PR checklist's doc-count sweep). |
| R-19 | Medium | DONE @34c89c8 — NPOI spikes banked HISTORICAL; Spike 6-Q established (quarterly SDK bump + GHSA floor re-verify + SpreadCheetah/ClosedXML/EPPlus scan), first due 2026-09-11 | `scheduled-spikes.md`: both quarterly spikes are NPOI-era, still showing "Next due 2026-08-16 (pending)" while the README calls them historical; the re-check discipline died with the swap and nothing replaced it. | NPOI spikes get HISTORICAL banners; new SDK-era cadence defined: quarterly `DocumentFormat.OpenXml` bump + GHSA advisory check (System.IO.Packaging CVEs are floor-patched as of 3.5.1 — re-verify quarterly) + competitive scan (SpreadCheetah/ClosedXML/EPPlus). First due date set. |
| R-20 | Medium | DONE @34c89c8 — DECIDED: amend design §4 + §8 layout to truth now (claim struck with dated amendment); the comparative suite itself stays scheduled as R-29 (SpreadCheetah + ClosedXML; NPOI-direct moot post-I-82) | Design §4 + roadmap claim a comparative benchmark suite ("vs NPOI direct, EPPlus, ClosedXML") that was never built — zero competitor references in `benchmarks/`. | Either build comparative benches (R-29) or amend design §4/roadmap to current truth. Decide here, then graduate. |
| R-21 | Minor | DONE @34c89c8 — flip step added to the release-PR checklist (next to the PublicAPI flip); the flip itself executes on the next release PR by construction | `AnalyzerReleases.Shipped.md` empty despite shipped releases carrying NXLS diagnostics; release checklist lacks the flip step. | Flip Unshipped→Shipped at next release; add the step to the release-PR checklist in `roadmap.md`. |
| R-22 | Medium | DONE @60f1614 — docs/interop.md: LO matrix (nightly-asserted), hyperlink-on-edit, 2GB ceiling, formula-injection guidance, docProps DECIDED stay-as-is (consumer-optional metadata; nondeterministic bytes; Underlying escape), sourcegen flow note | Missing interop/limits documentation (now evidence-backed): LO resave drops sheet `<autoFilter>`+criteria and workbook structure-lock (sheet password protection, merges, rich text, tables+totals, CF, comments, links, pictures, connectors all survive); hyperlink-on-edit semantics (R-10); System.IO.Packaging ~2GB single-part ceiling (top-voted SDK issue) as a streaming limit; formula-injection (DDE/CSV-style) guidance for untrusted data; `docProps/core+app` absent from packages ([verified] 2026-06-10 by part-list probe; legal, unconventional — decide emit-or-document); source-tree consumers must wire the SourceGen analyzer explicitly (bare `ProjectReference` doesn't flow it; NuGet consumers fine). | A `docs/interop.md` (or README section) carrying the verified matrix + limits; `docProps` decision recorded. |
| R-23 | Medium | DONE @202b432 — eleven recipes added under `samples/NetXlsx.Cookbook/`, each registered in `Program.cs` and paired with one `[Fact]` in `tests/NetXlsx.GoldenFiles/Recipes/PostV11RecipeSmoke.cs` (GoldenFiles 35→46 ×2 TFMs, full suite green): `conditional-formatting` (I-73), `charts` (I-75), `sorting-ranges` (I-72), `split-and-freeze-panes` (I-70), `row-and-column-grouping` (I-71), `shapes-and-connectors` (I-74/I-80), `picture-borders` (I-86), `totals-rows` (I-64), `autofilter-criteria` (I-66/I-68), `named-style-integration` (I-67), `macro-enabled-passthrough` (I-69). Smoke tests assert via the public read-back API where one exists (CF count, sorted values, `IPicture.Border`, `IsMacroEnabled`, rehydrated `RegisteredStyleNames`) and via the persisted OOXML part (SavedOoxml inspector) otherwise (chart graphicFrames, pane state, outlineLevel, cxnSp, filterColumn, totalsRowFunction). Doc-count sweep: README's two hand-maintained recipe counts ("20 — 13 v1.0 + 7 v1.1") and the now-false "post-v1.1 recipes tracked on the roadmap" caveat replaced with directory + `cookbook --help` live tally (no-drift discipline, same as test/decision counts). **JUDGMENT CALL / FINDING (operator, doc-truth):** the ledger framed item #10 as "named-style **OOXML integration**" and the surface first looked like the in-process-only I-57 convenience — but **I-67 (v1.3) already closed that loop**: `RegisterStyle` writes a `<cellStyle>` entry to styles.xml and `Workbook.Open` rehydrates the name→style map (pinned by NamedStyleTests `Registered_Names_Survive_File_Roundtrip` + the new recipe). The recipe therefore demonstrates the *true* round-tripping behavior. STALE DOCS this surfaces (NOT fixed here — operator doc-truth territory, like the §6.2 drifts): design.md §6.2.2 I-57 paragraph still asserts "the name → style map is not rehydrated"; and the v1.1 `BrandedStyles.cs` recipe header comment still says "the name -> style map is not rehydrated by Workbook.Open" — both contradicted by shipped I-67. **→ FIXED in S18 @72ad4d0 (doc-truth pass): both sites corrected, plus the higher-impact `IWorkbook.RegisterStyle` XML-doc (shipping IntelliSense) and the `roadmap.md` item; the xfId-back-reference nuance preserved per §6.2.6.** | New recipes (each doubling as a golden test, per the established pattern) covering the v1.2–v2.0 surface; counts updated everywhere by the doc-count sweep. |

## C. Engineering hardening (CI / packaging / test depth)

| ID | Sev | Status | Finding | Done when |
|----|-----|--------|---------|-----------|
| R-24 | Medium | DONE @7ab8acb — EnablePackageValidation on; baseline version deferred-with-rationale (package id not on nuget.org — verified 2026-06-12; R-31 owns publish) + release-checklist bump step added; pack verified | `EnablePackageValidation` + `PackageValidationBaselineVersion=2.0.1` absent — the highest-value missing pack gate for a multi-TFM library (binary/cross-TFM drift that PublicApiAnalyzers can't see). | Enabled in `NetXlsx.csproj`/`Directory.Build.props`; pack passes; release checklist notes the baseline bump per release. |
| R-25 | Medium | DONE @60f1614 — tools/NetXlsx.InteropProbe (promoted review harness + hard-assert modes) + interop-nightly.yml (LO pdf canary + resave -> survivor asserts -> openpyxl read-only leg); all legs verified green locally vs LO 26.2; also discharges the DoD's LO-smoke line | Third-party-writer corpus is untested in CI — exactly the class that caught R-6/R-7/R-8. LibreOffice headless is scriptable (this review automated it; harness at `.tmp/repro`, LO at `~/opt/lo/.../soffice`). | Nightly (not per-PR) workflow: generate kitchen-sink → LO convert (pdf + xlsx resave) → NetXlsx re-reads resave → probe assertions; `apt-get libreoffice-calc` on the runner. Also satisfies the DoD's "LibreOffice smoke test" line with automation. |
| R-26 | Medium | DONE @8fd87f6 — fuzz-nightly.yml (20k-iteration sweep + 2k-seed corpus bit-flip, env-scaled; PR smoke unchanged); corpus committed AS CODE (deterministic builders per emission path — same reproducibility, no binary-fixture provenance overhead; recorded deviation from the 'committed corpus seed set' phrasing); findings persist their failing input as artifacts; first scaled run found R-37 (fixed @b4ee24f before landing) | Fuzz depth vs stated intent: 18 tests/~400 ms smoke vs roadmap's "long-running on a nightly cadence" (`roadmap.md:243`). | Scheduled nightly deep-fuzz workflow (high iteration count, committed corpus seed set); PR smoke unchanged. |
| R-27 | Minor | DONE @7ab8acb — decided: wire fully (coverlet in all four projects, collector verified producing cobertura) + informational per-assembly coverage table in the CI job summary; no hard gate (recorded) | Coverage wiring inconsistent: coverlet only in `NetXlsx.Tests` (other three projects emit collector warnings every run); cobertura artifact uploaded but never reported or gated. | Either coverlet in all four + a CI report step (no hard gate required), or remove the dead collector flags; decision recorded. |
| R-28 | Minor | DONE @7ab8acb — macos-latest leg added; action-gh-release SHA-pinned (v3 tag resolved live); SourceLink comment replaced with verified truth (snupkg PDBs embed source-link JSON, inspected both TFMs) | CI matrix has no macOS leg; release action `softprops/action-gh-release@v3` not SHA-pinned; stale "re-add Microsoft.SourceLink" comment in `Directory.Packages.props:46-49` (SourceLink ships in the .NET 8+ SDK — flags already set, just remove the TODO and verify pack output). | One small infra slice: mac leg added, action SHA-pinned, comment removed, sourcelink verified in the snupkg. |
| R-29 | Medium | DONE @f467065 — `Comparative.cs` (NetXlsx.Comparative ns — OUT of the `--filter "*Benchmarks*"` CI gate by namespace, verified via `--list flat`) measures NetXlsx vs SpreadCheetah 1.27.0 (streaming write, 100k×10) + ClosedXML 0.105.0 (buffered write 50k×10 + read); bench-only deps pinned centrally (never ship). Real local run 2026-06-18 (i9-12900, .NET 10.0.2, ShortRun): **NetXlsx slower on every path** — ~26× vs SpreadCheetah streaming (~255× more alloc), ~2.4× vs ClosedXML buffered, ~1.35× vs ClosedXML read; absolute §4 targets still hold on extrapolation. Table+methodology in benchmarks/README.md; §4 amended (incl. §10 stale NPOI-overhead line). **JUDGMENT CALL / FINDING (operator):** (a) NetXlsx underperforms both comparators — comparison data is unflattering and recorded as such, not buried; (b) streaming-write churns ~1 GB transient alloc for 1M cells (`OpenXmlWriter` path) — flagged as a candidate roadmap perf item, NOT fixed here (R-29 is measurement-only). NOTE: benchmark numbers are box-specific; absolute ms differ on other hardware. | Test-side-only deps; comparative bench suite + README-able results table; design §4 amended to match reality. |
| R-30 | Minor | **DONE @b5b985b (S21)** — control-char/lone-surrogate gap closed earlier by I-88's `ControlCharacterTests`; the remainder landed S21: **`LocaleMatrixTests`** (tr-TR / Turkish-I — serialization stays culture-invariant under a comma-decimal ambient culture [`<v>` = `1234.5` not `1234,5`]; sheet + named-style lookups are `OrdinalIgnoreCase`, immune to the tr-TR fold [`FILE`/`file` match; dotted `I`/dotless `ı` stay distinct]; date/number round-trip invariant; DisplayCulture=tr-TR read path) + **`ConcurrencyStressTests`** (strict-mode mixed-mutation stress that SAVES + REOPENS intact — the integrity gap beyond the existing no-throw/no-tear detection contract — plus save-vs-mutate). **FINDING + FIX (operator):** the save-vs-mutate stress surfaced a real pre-existing defect — `Save` enumerated `_sheetsByIndex` + cloned the part graph WITHOUT the mutation lock, so a `Save` racing a mutation threw a raw `InvalidOperationException` ("Collection was modified"). Fixed in the same slice: `Save` now `EnterMutation()`s like every workbook-wide op (mirrors Dispose/R-15) — strict mode serializes, default mode surfaces the discoverable concurrency exception. Suite 1793→1804 ×2 TFMs. | Tests for encoding edges missing (control chars, lone surrogates) — emoji/tab/newline verified fine; tr-TR/locale matrix thin (de-DE + invariant only); no thread-safety stress beyond detection contract. | Folded into R-3's test work + one locale/concurrency test slice; scope deliberately bounded. |
| R-36 | Minor | DONE @7ab8acb — decided ADD (not WONT-FIX): README/CONTRIBUTING invite issues+PRs, so templates are owed; minimal bug/feature/PR-checklist set landed mirroring CONTRIBUTING's gates | NEW (verification pass): no issue templates / PR template under `.github/` — the one review finding (Tier-4 §16) this ledger had dropped. Recorded per the ledger's own rule (nothing from the review goes untracked). | Add minimal bug/feature issue templates + a PR-checklist template in the S7 infra slice, or WONT-FIX with the explicit rationale that a pre-publication solo repo doesn't take external contributions yet. |

## D. Operator decisions (no code until decided — present as one memo)

| ID | Status | Decision needed | Context |
|----|--------|-----------------|---------|
| R-31 | **DECIDED 2026-06-11** (sign-off on the amended memo): reservation email to support@nuget.org (exact ID + `NetXlsx.*` prefix) now + early real alpha as the durable claim — the original placeholder idea was policy-prohibited (squatting clause, verified verbatim). Execution = operator actions, PENDING | **Claim the `NetXlsx` nuget.org ID?** It is unclaimed (404, verified 2026-06-10); the name has invested identity; squatting is unguarded until first push. Publication itself stays operator-gated (standing decision), but ID reservation (0.0.x placeholder or prefix reservation) is a separable, cheap act. | Review §Tier-4 |
| R-32 | **DECIDED 2026-06-11**: adopt Trusted Publishing + provenance attestation at publish time. Owner-scoped (docs verbatim) so NO dependency on R-31; attestation = .signature.p7s-injection caveat as reworded in the memo; confirm the TP option appears in the account UI before committing the workflow | **Release workflow modernization at publish time:** NuGet Trusted Publishing (OIDC, `NuGet/login@v1`) instead of long-lived `NUGET_API_KEY`; `actions/attest-build-provenance` on release artifacts (nuget.org re-signs nupkgs — attest the GH-release artifacts, document the caveat). | Only matters when publishing becomes real; decide alongside R-31. |
| R-33 | **DECIDED 2026-06-11** (verdicts confirmed at sign-off; feed the R-18 re-baseline in S5): theme styling = I-89; streaming read PROMOTE; `In(...)` 3+ PROMOTE (small); Top-N schedule behind those; threaded comments DEMOTE/hold | **Post-swap feature triage** (the dead-NPOI-blocker list from R-18): threaded comments, streaming read (`OpenXmlPartReader`), full theme-color styling (`CellStyle.FontColorTheme` — pairs with R-8 and XlsxCodeGen Appendix A #1), Top-N autofilter, `In(...)` 3+ values (probe-verified model-shape-only on the SDK engine). Which get scheduled, which get demoted? | Feeds the R-18 re-baseline verdicts. |
| R-34 | **DECIDED 2026-06-11** (defaults confirmed at sign-off; apply at the R-18 re-baseline in S5): migration guide first; pivot write = headline v3.0 bet; encryption as add-on package; IAsyncEnumerable bundles with streaming read; AutoSize fallback opportunistic | **Strategic feature bets** (demand-ranked from ecosystem research): pivot-table authoring (v3.0; competitors chronically broken here; preservation-first architecture is advantaged), agile encryption via MS-OFFCRYPTO (v3.0; NPOI blocker dead; plausible add-on package), template-based export (MiniExcel's moat), `IAsyncEnumerable` async streaming, AutoSize approximate-metrics fallback for non-embedded fonts. EPPlus-8 license keys + ClosedXML's 13-month stable gap = active migration window; "coming from EPPlus/ClosedXML" guide. | Roadmap material — decide at the R-18 re-baseline session. |

---

## Execution plan — slices in order

Ordering principle: data-integrity defects first (charter §1.2/§1.4), then the
truth pass (cheap, protects the brand), then correctness, then hardening,
with design-gated items proposed early so operator sign-off never blocks the
pipeline. One slice = one session = one coherent commit set, per the
established working agreement.

| Slice | Contents | Gate |
|-------|----------|------|
| **S0** | Commit this ledger. | — |
| **S1** ✔ @6926552 | **R-35 first** (autosave-off so a failed save no longer poisons Dispose — R-1's test depends on it), then R-1 + R-2 atomic save (both engines) + failure-mode tests. Smallest possible diff; no API change. (Landed: 8 tests in `SaveFailureModeTests`, suite 1480+35+18+1 ×2 TFMs green, goldens byte-identical.) | none |
| **S2** ✔ memo delivered | **Design-proposal batch** (no code): R-3 control-char policy, R-8 theme cluster, R-11 removal family, R-12 sheet lifecycle — four I-NN shape proposals in one memo + the R-31..R-34 operator decisions. (Delivered: `docs/s2-design-proposals-2026-06-10.md` — I-88 control-char escape/fail-fast split, I-89 lazy-default-theme + styling symmetry, I-90 sheet lifecycle with sheet-reference lexer, I-91 removal family in 2 slices, R-31..R-34 recommendations + sign-off checklist. AMENDED 2026-06-11 after an external review, all material claims independently re-verified — CR-loss repro, AddPicture no-dedup, OoxmlTable stale-gap, golden grep, x14 gap, nuget policy + trusted-publishing docs verbatim; amendments marked [A-2026-06-11] in the memo.) | **SIGNED OFF AS AMENDED — operator, 2026-06-11.** S2 COMPLETE. S10+ unblocked; I-90 lands as 2 slices |
| **S3** ✔ @0e7c467+c7f9451 | R-4 + R-5 source-gen ReadRows fix (positional records, call shape, diagnostic, compile-and-run emission tests) + README typed-mapping example correction. (Landed: primary-ctor construction, NXLS0007, struct null-guard fix, 10 tests in `PositionalRecordEmissionTests` + full-reference harness path; suite 1490+35+18+1 ×2 TFMs green; goldens untouched.) | none (generator behavior, not surface) |
| **S4** ✔ @42206d7 | R-6 + R-7 read-fidelity fixes (serial rounding; **R-7 is now the full design-I7 conformance fix** — cached formula results across all typed getters + error literals in GetString, with crafted/LO-authored fixtures and a compile-and-run ReadRows-over-formula-column test); R-13 `<dimension>` emission **[golden-regen]**. (Landed: 19 tests across `DateTimeApiTests`/`FormulaCachedValueTests`/`DimensionTests`; suite 1509+35+18+1 ×2 TFMs; openpyxl read-only oracle verified live; golden-regen vacuous — no byte goldens committed.) | none |
| **S5** ✔ @34c89c8 | R-17 + R-18 + R-19 + R-20-decision + R-21 docs truth pass (README, roadmap re-baseline, scheduled-spikes successor, analyzer-release checklist step). (Landed: every claim re-verified at HEAD; matrix verdict block; Spike 6-Q due 2026-09-11; extra finds fixed in-scope: CONTRIBUTING test-count/three-projects claim, net9.0 TFM claims in both docs, roadmap drawings-section contradiction, GetValue<T> deviation recorded.) | R-18 verdicts used S2's R-33 answers |
| **S6** ✔ @8a2c419 | R-9 sheet-name tightening + R-14/R-15/R-16 verify-and-fix sweep (each ends DONE or WONT-FIX with rationale). (Landed: every item DONE — R-16's number-format leg is a recorded WONT-VALIDATE decision, not a fix; suite 1544+35+18+1 ×2 TFMs; 35 new test cases.) | none |
| **S7** ✔ @7ab8acb | R-24 + R-27 + R-28 + R-36 infra slice (PackageValidation, coverage wiring, mac leg, SHA-pin, SourceLink comment, templates-or-WONT-FIX). (Landed: all four DONE; note the mac leg + coverage summary prove out on the next CI run — local verification covered pack, collector attachments, and snupkg source-link.) | none |
| **S8** ✔ @60f1614 | R-25 LO interop nightly job + R-22 `docs/interop.md` (the job's probe set IS the doc's evidence). (Landed: probe tool in sln, 4 assert modes, nightly workflow with openpyxl leg; note the workflow itself proves out on its first scheduled run — local verification covered every assertion leg.) | none |
| **S9** ✔ @8fd87f6 | R-26 nightly deep fuzz. (Landed: env-scaled harness + corpus + finding-dump + workflow; paid for itself immediately — found R-37, fixed @b4ee24f with R-38 chartsheet decision recorded for the queue.) | none |
| **S10+** (S10 ✔ @f980076, S11 ✔ @940164d, S12 ✔ @81ce04e, S13 ✔ @c004788, S14 ✔ @98cac83, S15 ✔ @b557e4c, S16 ✔ @f467065, S17 ✔ @202b432, S18 ✔ @72ad4d0, S19 ✔ @ccf2e56, S20 ✔ @daadf58, S21 ✔ @b5b985b, S22 ✔ de-NPOI doc-mechanics §6.2.x cluster) | Signed-off I-NNs from S2 land in order: ~~R-3 control chars (I-88)~~ DONE @f980076 → ~~R-8 theme part (I-89)~~ DONE @940164d → ~~R-12 sheet lifecycle (I-90, 2 slices: rename+move DONE @81ce04e [S12] → delete DONE @c004788 [S13])~~ **I-90 COMPLETE** → ~~R-11 removal family (I-91, 2 slices: cell/workbook DONE @98cac83 [S14] + R-10 docs fold-in DONE → drawing layer DONE @b557e4c [S15])~~ **I-91 COMPLETE** → ~~R-29 comparative benches DONE @f467065 [S16]~~ → ~~R-23 cookbook recipes DONE @202b432 [S17]~~ → ~~S18 doc-truth pass DONE @72ad4d0 (named-style I-67 round-trip truth ×4 sites incl. shipping IntelliSense; I-89 §6.12.4 fold; §10 de-NPOI line; R-8 + R-12 judgment calls RATIFIED)~~ → ~~S19 §6.2 sketch reconciliation DONE @ccf2e56 (built `AddSheet(name,index)` [1-based, = AddSheet+MoveSheet] + `IReadOnlyList<ISheet> Sheets` [0-based snapshot, incl hidden]; dropped `IAsyncDisposable` marker; the 3 PENDING §6.2 drifts CLOSED; suite 1775→1785; 2 PublicAPI.Unshipped entries)~~ → ~~S20 R-38 chartsheet/dialogsheet open-with-visibility DONE @daadf58 (I-92 signed off + landed; ISheet.Kind + OoxmlChartsheet placeholder + IOoxmlSheet abstraction; suite 1785→1793)~~ **R-38 CLOSED** → ~~S21 R-30 remainder DONE @b5b985b (tr-TR/locale matrix + concurrency stress; surfaced+fixed the Save-not-locked defect)~~ **R-30 CLOSED**. **QUEUE DRAINED of agent slices — every R-row is DONE or DECIDED.** Remaining are OPERATOR-EXECUTION items, not agent work: R-31 (nuget ID reservation email) + R-32 (Trusted Publishing workflow) execution PENDING; plus carried doc-truth flags (de-NPOI doc-mechanics debt: **§6.2.x feature cluster RESOLVED @S22** — §4 S29 date styles, §6.2.2 I-57, §6.2.6 I-67, §6.2.7 I-69, §6.2.8 I-71, §6.2.9 I-73, §6.2.10 I-75, §6.2.11 I-78, §6.2.12 I-76, and the I-65 password section rewritten to the shipped SDK mechanics [verified against code], suite unchanged 1804+46+19+1 ×2; **REMAINING** = the later §6.2.13+/§6.4.x feature sections [connectors/shapes/split-pane/streaming/tables/RemoveTable/pictures/sheet-protection/data-validation/autofilter/rich-text + the async/formula notes + the ICell/IRow `.Underlying` sketches], denser with genuinely-historical narrative [I-80 connector bug-fix history, I-63 RemoveTable v1.2 NPOI-reflection record, I-49 streaming-cell rationale, the "NPOI surprise" notes] and API-shape judgment calls — proposed as a follow-up slice S23 with the judgment calls flagged for operator decision; §3 decision-table #32 [`Underlying` returns `XSSFWorkbook`] left untouched per the historical-record rule [§6.2 sketch + §10 already carry the corrected live SDK statement]; an Excel/LO-authored chartsheet golden fixture nice-to-have) and the R-29 streaming ~1 GB-alloc roadmap perf candidate. **This file is ready for its HISTORICAL banner once the operator closes out R-31/R-32 + the de-NPOI doc-mechanics follow-up (S23) — operator call, not auto-applied.** | sign-off SATISFIED 2026-06-11 for all four (amended memo) |

**Standing rules while draining:** every slice updates this ledger's Status
column in the same commit; CHANGELOG entries reference R-IDs; the two-pass
verification rule applies before any item flips to DONE; no new findings get
fixed ad-hoc — they get an R-ID first, then a slice.

---

## Reusable verification assets (from the review session)

- Kitchen-sink harness: `.tmp/repro` — `dotnet run -- gen /tmp/netxlsx-lo`
  (writes kitchen/streaming/apos workbooks exercising the full surface),
  `dotnet run -- verify <file>` (PROBE lines; diffable across round-trips).
  Worth promoting into the repo as the R-25 job's generator.
- LibreOffice 26.2.4 portable: `~/opt/lo/opt/libreoffice26.2/program/soffice
  --headless -env:UserInstallation=file:///tmp/lo-profile --convert-to pdf|xlsx`.
  (Mirror note: `download.libreoffice.org` is DNS-blocked in this dev env;
  `ftp.osuosl.org/pub/tdf/` works.)
- Python oracles: openpyxl 3.1.5 + python-calamine (user-site installed).
- From the verification pass (patterns worth reusing; `/tmp` copies are
  ephemeral): crafted-cell zip injection is the way to author third-party
  shapes NetXlsx's own writer can't produce (formula-cached `<v>`, 15-digit
  serials, `@r`-less rows) — NOTE NetXlsx serializes `x:`-PREFIXED
  spreadsheetML, so injected elements must carry the `x:` prefix or the
  edit no-ops silently; the positional-record repro is a 20-line project
  with the README's exact `[Worksheet]` record + an
  `OutputItemType="Analyzer"` ProjectReference (CS7036 in the `.g.cs`,
  CS1061 on `ReadRows<T>()`). Both belong in the R-4/R-7 slices as real
  fixtures.
