using System;

namespace NetXlsx.Cookbook;

/// <summary>
/// Cookbook entry point. Real recipes (per design §8.1) land during v1.0
/// implementation. Each recipe is a method here and a golden-file test in
/// <c>tests/NetXlsx.GoldenFiles/</c>.
/// </summary>
public static class Program
{
    public static int Main(string[] args)
    {
        Console.WriteLine("NetXlsx cookbook — scaffold placeholder.");
        Console.WriteLine("Recipes ship in v1.0 per docs/design.md §8.1.");
        return 0;
    }
}
